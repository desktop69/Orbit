{% extends 'base.html.twig' %}

{% block title %}Edit Performance{% endblock %}

{% block body %}
    

      <div class="container">
            <a class="btn btn-primary" href="{{ path('performance_show', {'id': performance.id}) }}"><i class="fas fa-arrow-left"></i> Back to performance</a>

    <div class="card o-hidden border-0 shadow-lg my-5">
      <div class="card-body p-0">
        <!-- Nested Row within Card Body -->
        <div class="container">  
          <div class="col-lg-15">
            <div class="p-10">
            <div class="text-center"> 
            <br> 
                <h1 class="h4 text-gray-900 mb-4"> Edit Performance !</h1>
            </div>


    {{ include('performance/_form.html.twig', {'button_label': 'Update'}) }}

            </div>
          </div>
        </div>
      </div>
    </div>
</div>
</div>
{% endblock %}
