{% extends 'admin/base.html.twig' %}

{% block title %}New PriceGn{% endblock %}

{% block body %}

      <div class="container">
<a class="btn btn-primary" href="{{ path('app_generateprices') }}"><i class="fas fa-arrow-left"></i> Back to list</a>
    <div class="card o-hidden border-0 shadow-lg my-5">
      <div class="card-body p-0">
        <!-- Nested Row within Card Body -->
        <div class="container">


          <div class="col-lg-15">
            <div class="p-10">
            
            <div class="text-center">  
            <br>
                <h1 class="h4 text-gray-900 mb-4"> Create New Generation !</h1>
            </div>

                {{ form_start(formpng) }}
                  {{ form_widget(formpng) }}
                  <br>
                  <hr>
                  <br>
                  <button class="btn  btn-primary btn-block">{{ button_label|default('Save') }}</button>
                  <br>
              {{ form_end(formpng) }}


         
            </div>
          </div>
        </div>
      </div>
    </div>
</div>


{% endblock %}
