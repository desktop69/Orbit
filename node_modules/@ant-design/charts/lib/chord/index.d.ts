import React from 'react';
import { ChordOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface ChordConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const ChordChart: React.ForwardRefExoticComponent<ChordConfig & React.RefAttributes<unknown>>;
export default ChordChart;
